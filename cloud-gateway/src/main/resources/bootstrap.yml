spring:
  cloud:
    nacos:
      discovery:
        server-addr: 39.106.8.177:8848
      config:
        server-addr: 39.106.8.177:8848
        file-extension: yml
    gateway:
      discovery:
        locator:
          # 开启基于服务发现的默认路由规则
          enabled: true
  application:
    name: cloud-gateway